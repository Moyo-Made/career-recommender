Flask==3.1.3
flask-cors==6.0.2
pymongo==4.17.0
python-dotenv
# Pinned to the exact versions the model was trained/pickled with so
# joblib.load() on the server matches local and doesn't warn or break.
joblib==1.5.3
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.8.0
PyJWT==2.10.1
# Production WSGI server (Flask's built-in server is dev-only).
gunicorn==23.0.0
