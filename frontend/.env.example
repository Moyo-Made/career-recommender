# Base URL of the backend API (no trailing slash).
# Local dev: leave this unset and the app defaults to http://127.0.0.1:5000
# Production: set VITE_API_URL in the Vercel dashboard to your Render URL, e.g.
#   https://career-recommender-api.onrender.com
VITE_API_URL=http://127.0.0.1:5000
